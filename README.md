# Employee Management System

---

## 📌 Project Description
Full-stack **Employee Management System** using **ASP.NET Core Web API** (backend) and **Angular** (frontend) with complete **CRUD operations**.  
The backend handles business logic and database operations using **Entity Framework Core** and **SQL Server**, while the frontend provides a responsive UI using **Angular, TypeScript, HTML, CSS, and Bootstrap**.

---

## ⚙️ Technologies Used

**Backend**
- ASP.NET Core Web API  
- C#  
- Entity Framework Core  
- SQL Server  
- RESTful API architecture  

**Frontend**
- Angular  
- TypeScript  
- HTML5  
- CSS3  
- Bootstrap  

---

## ✨ Features
- Add new employee details  
- View employee list  
- Update existing employee information  
- Delete employee records  
- RESTful API integration with Angular  
- Client-server separation (API & UI)  
- Clean code structure and best practices  

---

## 🗄️ Database
- SQL Server  
- Code First approach using Entity Framework Core  
- Proper table relationships and validations  

---

## 🔗 API Endpoints
- `GET /api/employees` – Get all employees  
- `GET /api/employees/{id}` – Get employee by ID  
- `POST /api/employees` – Add new employee  
- `PUT /api/employees/{id}` – Update employee  
- `DELETE /api/employees/{id}` – Delete employee  

---

## 🚀 How to Run the Project
1. Clone the repository  
2. Configure SQL Server connection string in `appsettings.json`  
3. Run the **ASP.NET Core API**  
4. Navigate to the Angular project folder  
5. Run `npm install`  
6. Run `ng serve`  
7. Open browser and access **http://localhost:4200**  

---

## 📌 Use Case
This project is ideal for:
- Learning full-stack development  
- Understanding Angular–ASP.NET Core integration  
- Practicing RESTful APIs and CRUD operations  
- Building portfolio-ready projects  

---

